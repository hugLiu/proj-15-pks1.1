@*该模板并不用于创建主文章，而只用作Base_Article.TargetArticles这个集合的编辑器
    而它的主要目的也只是编辑文章的特殊属性集Exts*@
@using Jurassic.CommonModels.Articles;
@using Jurassic.CommonModels;
@using Jurassic.WebRepeater;
@model Base_Article
@{
    Model.Exts = Model.Exts.OrderBy(ext => SiteManager.Catalog.GetExtById(ext.CatlogExtId).Ord).ToList();
}
<tr>
    @Html.HiddenFor(model => model.Id)
    @Html.ExtsEditor(new ExtRenderEvent { Inline = true })
</tr>